import Button1 from "../components/Buttons/Button1";
import Button2 from "../components/Buttons/Button2";
import React from "react";
export default function Home() {
  return (
    <div className="items-center justify-center flex p-20">
      <Button1 />
    </div>
  );
}

/* 

Import first the component

Add de component <Component name /> like this inside the div

If the component works right delete the importation in both sides , this is only for testing

*/
